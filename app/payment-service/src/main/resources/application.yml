server:
  port: 8085
grpc:
  server:
    port: 9095
  client:
    support-service:
      address: ${GRPC_CLIENT_SUPPORT_SERVICE_ADDRESS:${GRPC_SERVER_SUPPORT_SERVICE:static://localhost:9096}}
      negotiation-type: plaintext
      enable-keep-alive: true
      keep-alive-without-calls: true

spring:
  application:
    name: payment-service
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  threads:
    virtual:
      enabled: true
  kafka:
    bootstrap-servers: ${SPRING_KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: 6379
      timeout: 2000
      repositories:
        enabled: false
  sql:
    init:
      schema-locations: classpath:schema.sql
      mode: ${SPRING_SQL_INIT_MODE:never}
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/ecommerce_payment}
    username: ${SPRING_DATASOURCE_USERNAME:root}
    password: ${SPRING_DATASOURCE_PASSWORD:49@Da5J,[KsemDN}
  messages:
    basename: i18n/messages
    encoding: UTF-8


kafka:
  consumer:
    group-id: ${KAFKA_CONSUMER_GROUP_ID:payment-service}
    max-poll-records: ${KAFKA_CONSUMER_MAX_POLL_RECORDS:300}
    concurrency: ${KAFKA_CONSUMER_CONCURRENCY:6}


management:
  endpoint:
    health:
      probes:
        enabled: true
      show-details: always
  endpoints:
    web:
      exposure:
        include: "*"
  otlp: # Vì sẽ push metrics, traces qua cho Otel Collector nên ở đây phải là otlp.
    metrics:
      export:
        url: ${OTLP_METRICS_EXPORT_URL:http://localhost:4318/v1/metrics}
        step: 5s
  health:
    readinessstate:
      enabled: true
    livenessstate:
      enabled: true
    circuitbreakers:
      enabled: true
    ratelimiters:
      enabled: true
  metrics:
    distribution:
      percentiles-histogram:
        resilience4j:
          circuitbreaker:
            enabled: true
        http:
          server:
            requests:
              enabled: true

#Eureka client config.
eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_CLIENT_SERVICEURL_DEFAULTZONE:http://localhost:8761/eureka/}
      preferIpAddress: true
    register-with-eureka: true
    fetch-registry: true


logging:
  level:
    net.devh.boot.grpc.server: DEBUG

resilience4j:
  timelimiter:
    instances:
      support-service:
        timeout-duration: 5s
        cancel-running-future: true
  circuitbreaker:
    instances:
      support-service:
        register-health-indicator: true
        sliding-window-size: 30
        minimum-number-of-calls: 30
        failure-rate-threshold: 50
        wait-duration-in-open-state: 20000
        permitted-number-of-calls-in-half-open-state: 15
        sliding-window-type: COUNT_BASED

paypal:
  base-url: ${PAYPAL_BASE_URL:https://api-m.sandbox.paypal.com}
  client-id: ${PAYPAL_CLIENT_ID:AcT4YNcb8TKxujcMFSXjxf5yLs4DlG4NIsXBTRQNXOZR229fEuyYrWHLMWLh48n8tqtNH5vYSybVqTR2}
  client-secret: ${PAYPAL_CLIENT_SECRET:EO62CNg7sg3egDMcTJ3SsnODv9zzpVxi2hQTmRrertovRX-qtj9MO553_cekFriMY7Zf1dKA3uVCFCyZ}
  brand-name: ${PAYPAL_BRAND_NAME:e-commerce}
  locale: ${PAYPAL_LOCALE:en-US}
  return-url: ${PAYPAL_RETURN_URL:http://localhost:8085/api/payments/paypal/callback/success}
  cancel-url: ${PAYPAL_CANCEL_URL:http://localhost:8085/api/payments/paypal/callback/cancel}
  frontend-success-url: ${PAYPAL_FRONTEND_SUCCESS_URL:http://localhost:5173/orders}
  frontend-cancel-url: ${PAYPAL_FRONTEND_CANCEL_URL:http://localhost:5173/orders}
  frontend-failure-url: ${PAYPAL_FRONTEND_FAILURE_URL:http://localhost:5173/orders}
  country-code: ${PAYPAL_COUNTRY_CODE:VN}
  sse-timeout-ms: ${PAYPAL_SSE_TIMEOUT_MS:1800000}
